# app/backend-ingress/kustomization.yaml 생성
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: codecooking
  app.kubernetes.io/component: backend
